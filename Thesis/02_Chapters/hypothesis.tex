\section{Hypothesis}

The inclusion of more than two layers of duplicated weights, each with their respective learning rates, can reduce the forgetting of previously learned information in an incremental learning model using Multilayer Perceptron (MLP) artificial neural networks when the Backpropagation algorithm is applied.
